# fly.toml app configuration file generated for nexordle-renderer on 2026-02-02T12:52:49Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'nexordle-renderer'
primary_region = 'cdg'

[build]

[http_service]
  internal_port = 3000
  force_https = true

  # 🔥 IMPORTANT : renderer toujours actif
  auto_stop_machines = false
  auto_start_machines = false
  min_machines_running = 1

  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
